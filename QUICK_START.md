# 🚀 Быстрый старт

## Подготовка

### 1. Установка зависимостей

```bash
# Корневые зависимости
npm install

# Backend зависимости
cd backend && npm install && cd ..

# Frontend зависимости
cd frontend && npm install && cd ..
```

### 2. Запуск Docker (PostgreSQL + Redis)

```bash
docker-compose up -d
```

Проверка:
```bash
docker ps
```

Должны быть запущены:
- `montaj-postgres`
- `montaj-redis`

### 3. Настройка Backend

```bash
cd backend

# Скопировать example env
cp .env.example .env

# Применить миграции
npx prisma generate
npx prisma migrate dev --name init

cd ..
```

### 4. Запуск приложения

```bash
# Из корневой папки
npm run dev
```

Приложение запустится:
- **Frontend:** http://localhost:3000
- **Backend:** http://localhost:5000

## Проверка работоспособности

### Автоматические тесты

```bash
./test-system.sh
```

### Ручная проверка

#### Регистрация и вход

1. Откройте http://localhost:3000
2. Нажмите "Начать работу"
3. Зарегистрируйтесь как CUSTOMER или EXECUTOR
4. Подтвердите SMS (код будет в консоли backend)
5. Войдите в систему

#### Создание заказа (Заказчик)

1. Войдите как CUSTOMER
2. Перейдите в "Создать заказ"
3. Заполните форму:
   - Категория: любая
   - Заголовок: минимум 10 символов
   - Описание: минимум 20 символов
   - Бюджет: минимум 5000₽
4. Создайте заказ

#### Отклик на заказ (Исполнитель)

1. Войдите как EXECUTOR
2. Заполните профиль исполнителя (/profile)
3. Перейдите в "Доступные заказы"
4. Откликнитесь на заказ
5. Проверьте списание комиссии (баланс в дашборде)

#### Выбор исполнителя (Заказчик)

1. Войдите как CUSTOMER
2. Откройте свой заказ
3. Просмотрите отклики
4. Выберите исполнителя
5. Контакты станут видны обеим сторонам

## Основные маршруты

### Публичные
- `/` - главная страница
- `/register` - регистрация
- `/login` - вход

### Заказчик
- `/customer/dashboard` - дашборд заказчика
- `/orders/create` - создать заказ
- `/orders` - все заказы
- `/orders/[id]` - детали заказа

### Исполнитель
- `/executor/dashboard` - дашборд исполнителя
- `/orders` - доступные заказы
- `/orders/map` - карта заказов
- `/orders/[id]` - детали заказа с кнопкой отклика

### Общие
- `/profile` - профиль пользователя

## API Endpoints

### Аутентификация
- `POST /api/auth/register` - регистрация
- `POST /api/auth/verify-sms` - подтверждение SMS
- `POST /api/auth/login` - вход

### Заказы
- `GET /api/orders` - список заказов
- `POST /api/orders` - создать заказ
- `GET /api/orders/:id` - детали заказа
- `GET /api/orders/my/list` - мои заказы
- `POST /api/orders/:id/select-executor` - выбрать исполнителя
- `POST /api/orders/:id/complete` - завершить заказ

### Отклики
- `POST /api/responses` - создать отклик
- `GET /api/responses/order/:orderId` - отклики на заказ
- `GET /api/responses/my` - мои отклики

### Профиль
- `GET /api/users/profile` - мой профиль
- `PUT /api/users/profile` - обновить профиль
- `POST /api/users/upload-photo` - загрузить фото

## Тарифы исполнителей

При регистрации:
- **Премиум** на 1 месяц
- **1000₽** бонусов на балансе

Тарифы:
1. **Standard** - 150₽ за каждый отклик
2. **Comfort** - 500₽ только при выборе заказчиком
3. **Premium** - безлимитные отклики, 5000₽/месяц

## Возможные проблемы

### Docker не запускается
```bash
sudo systemctl start docker
docker-compose up -d
```

### Порты заняты
Проверьте, что порты 5432, 6379, 5000, 3000 свободны:
```bash
lsof -i :5432
lsof -i :6379
lsof -i :5000
lsof -i :3000
```

### Ошибка миграции
```bash
cd backend
npx prisma migrate reset
npx prisma migrate dev
```

### Тесты не проходят
Убедитесь, что Docker запущен и порты доступны.

## Документация

- `README.md` - общая информация
- `SETUP.md` - детальная установка
- `TEST.md` - запуск тестов
- `STAGE2_COMPLETE.md` - что реализовано в этапе 2
- `MAPS_SETUP.md` - настройка Яндекс.Карт
- `UPDATE_INSTRUCTIONS.md` - инструкция после перезапуска

## Поддержка

Все файлы проекта имеют подробные комментарии. При возникновении вопросов:
1. Проверьте логи в консоли
2. Посмотрите соответствующую документацию
3. Проверьте тесты для примеров использования
