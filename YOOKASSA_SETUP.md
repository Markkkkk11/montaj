# 💳 Настройка ЮKassa для платежей

## Что такое ЮKassa?

ЮKassa (ранее Яндекс.Касса) — это сервис приёма онлайн-платежей от Yandex. Позволяет принимать платежи банковскими картами, электронными кошельками и другими способами.

## Регистрация в ЮKassa

### 1. Создание аккаунта

1. Перейдите на https://yookassa.ru/
2. Нажмите "Подключиться"
3. Зарегистрируйтесь как самозанятый, ИП или юридическое лицо
4. Пройдите верификацию (загрузка документов)

### 2. Создание магазина

1. Войдите в личный кабинет ЮKassa
2. Перейдите в раздел "Магазины"
3. Нажмите "Добавить магазин"
4. Заполните информацию:
   - Название магазина
   - URL сайта
   - Категория товаров/услуг
   - Описание

### 3. Получение API ключей

1. В настройках магазина найдите раздел "API"
2. Сохраните:
   - **shopId** - идентификатор магазина
   - **Секретный ключ** - для подписи запросов

⚠️ **Важно:** Секретный ключ храните в безопасности!

### 4. Настройка Webhook

Webhook - это URL, на который ЮKassa отправляет уведомления о статусе платежей.

1. В настройках магазина найдите "HTTP-уведомления"
2. Укажите URL: `https://ваш-домен.ru/api/webhooks/yookassa`
3. Сохраните

## Настройка проекта

### 1. Переменные окружения

В файле `/home/mark/Documents/montaj/backend/.env` добавьте:

```env
# YooKassa
YOOKASSA_SHOP_ID=ваш-shop-id
YOOKASSA_SECRET_KEY=ваш-secret-key

# Frontend URL (для возврата после оплаты)
FRONTEND_URL=https://ваш-домен.ru
```

### 2. Проверка настроек

Проект настроен на работу с ЮKassa:
- ✅ API клиент: `backend/src/config/yookassa.ts`
- ✅ Сервис платежей: `backend/src/services/payment.service.ts`
- ✅ Webhook обработчик: `backend/src/routes/webhook.routes.ts`

## Тестирование

### Mock режим (без реальных ключей)

Если переменные `YOOKASSA_SHOP_ID` и `YOOKASSA_SECRET_KEY` не заданы, проект работает в mock-режиме:

- Создаются тестовые платежи
- Генерируются mock confirmationUrl
- Webhook можно имитировать вручную

**Mock режим подходит для:**
- ✅ Разработки
- ✅ Локального тестирования
- ✅ CI/CD тестов

**НЕ подходит для:**
- ❌ Продакшена
- ❌ Реальных платежей

### Тестовый режим ЮKassa

ЮKassa предоставляет тестовую среду:

1. В личном кабинете включите "Тестовый режим"
2. Используйте тестовые ключи
3. Тестовые карты для проверки:
   - **Успешная оплата:** `5555 5555 5555 4444`
   - **Отклонённая оплата:** `5555 5555 5555 5599`
   - CVV: любые 3 цифры
   - Срок: любая будущая дата

## Процесс платежа

### 1. Создание платежа

```typescript
POST /api/payments/top-up
{
  "amount": 1000
}
```

Ответ:
```json
{
  "success": true,
  "payment": {
    "id": "uuid",
    "amount": "1000.00",
    "status": "PROCESSING"
  },
  "confirmationUrl": "https://yoomoney.ru/checkout/..."
}
```

### 2. Оплата пользователем

Frontend перенаправляет пользователя на `confirmationUrl`:

```javascript
window.location.href = confirmationUrl;
```

Пользователь:
1. Видит форму оплаты ЮKassa
2. Вводит данные карты
3. Подтверждает платёж
4. Возвращается на сайт по `returnUrl`

### 3. Обработка результата

**Вариант A: Return URL (немедленный)**

После оплаты пользователь возвращается на:
```
http://ваш-домен.ru/profile/balance?tab=payments&payment_id=xxx
```

Frontend вызывает:
```typescript
GET /api/payments/success/callback?payment_id=xxx
```

**Вариант B: Webhook (надёжный)**

ЮKassa отправляет POST запрос на:
```
POST https://ваш-домен.ru/api/webhooks/yookassa
```

Backend автоматически обрабатывает платёж.

⚠️ **Важно:** Всегда полагайтесь на webhook, а не на return URL!

## Безопасность

### 1. Проверка IP адресов (продакшен)

Разрешите webhook только с IP ЮKassa:
```
185.71.76.0/27
185.71.77.0/27
77.75.153.0/25
77.75.156.11
77.75.156.35
77.75.154.128/25
2a02:5180::/32
```

Добавьте в `webhook.routes.ts`:
```typescript
const YOOKASSA_IPS = [
  '185.71.76.0/27',
  '185.71.77.0/27',
  // ... остальные
];

router.post('/yookassa', (req, res, next) => {
  const ip = req.ip;
  if (!isIPAllowed(ip, YOOKASSA_IPS)) {
    return res.status(403).send('Forbidden');
  }
  next();
}, paymentController.handleWebhook);
```

### 2. Проверка подписи (рекомендуется)

Проверяйте подпись webhook запросов от ЮKassa.

### 3. HTTPS обязателен

ЮKassa отправляет webhook только на HTTPS URL.

## Типичные ошибки

### Ошибка: "Invalid credentials"

**Причина:** Неверный shopId или secretKey

**Решение:**
1. Проверьте правильность ключей
2. Убедитесь, что используете ключи из нужного магазина
3. Проверьте, что магазин активирован

### Ошибка: "Webhook not received"

**Причина:** ЮKassa не может достучаться до вашего сервера

**Решение:**
1. Проверьте, что сервер доступен из интернета
2. Проверьте HTTPS сертификат
3. Проверьте firewall
4. Используйте ngrok для локального тестирования

### Ошибка: "Payment not found"

**Причина:** Платёж не найден в БД

**Решение:**
1. Проверьте, что платёж создался в БД
2. Проверьте корректность `yookassaPaymentId`
3. Проверьте логи

## Локальное тестирование с ngrok

Для тестирования webhook локально:

### 1. Установите ngrok

```bash
# Ubuntu/Debian
sudo snap install ngrok

# или скачайте с https://ngrok.com/
```

### 2. Запустите туннель

```bash
ngrok http 5000
```

Получите URL: `https://abc123.ngrok.io`

### 3. Настройте webhook в ЮKassa

Используйте: `https://abc123.ngrok.io/api/webhooks/yookassa`

### 4. Тестируйте

Теперь ЮKassa может отправлять webhook на ваш локальный сервер!

## Продакшен чеклист

Перед запуском в продакшен проверьте:

- [ ] ✅ Получены реальные API ключи
- [ ] ✅ Webhook URL настроен в ЮKassa
- [ ] ✅ HTTPS сертификат установлен
- [ ] ✅ Переменные окружения заданы
- [ ] ✅ Проверка IP webhook (опционально)
- [ ] ✅ Логирование платежей настроено
- [ ] ✅ Мониторинг ошибок настроен
- [ ] ✅ Backup БД настроен
- [ ] ✅ Протестированы все сценарии:
  - [ ] Успешная оплата
  - [ ] Отклонённая оплата
  - [ ] Отмена пользователем
  - [ ] Возврат средств

## Документация ЮKassa

- **Официальная документация:** https://yookassa.ru/developers
- **API Reference:** https://yookassa.ru/developers/api
- **Webhook:** https://yookassa.ru/developers/using-api/webhooks
- **Тестовые данные:** https://yookassa.ru/developers/payments/testing

## Поддержка

При возникновении проблем:

1. **Проверьте логи:** `backend/logs/` (если настроено)
2. **Проверьте БД:** таблицы `payments` и `transactions`
3. **Техподдержка ЮKassa:** support@yookassa.ru

---

**Готово!** Теперь ваш проект готов принимать реальные платежи через ЮKassa! 🎉

