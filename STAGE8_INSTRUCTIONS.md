# 📋 ЭТАП 8: ИНСТРУКЦИЯ ПО ЗАПУСКУ

## 🎯 Что было добавлено

В этом этапе добавлены последние критичные функции из технического задания:

1. **"Приступить к работе"** - исполнитель подтверждает начало работы
2. **"Отказаться от заказа"** - исполнитель может вернуть заказ заказчику
3. **Real-time чат** - мгновенный обмен сообщениями между заказчиком и исполнителем

---

## 🚀 Быстрый запуск

### Шаг 1: Установка зависимостей

```bash
# Backend
cd backend
npm install

# Frontend
cd ../frontend
npm install
```

### Шаг 2: Миграция базы данных

**Вариант A: Автоматическая миграция (если БД уже настроена)**

```bash
cd backend

# Если нужно, установите DATABASE_URL
export DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/montaj"

# Применить миграцию
npx prisma migrate dev --name add_work_started_and_chat

# Сгенерировать Prisma Client
npx prisma generate
```

**Вариант B: Ручная миграция SQL**

Если автоматическая миграция не работает, выполните SQL вручную:

```bash
psql -U YOUR_USER -d montaj -f backend/prisma/migrations/manual_add_work_started_and_chat.sql
```

Или скопируйте SQL из файла `backend/prisma/migrations/manual_add_work_started_and_chat.sql` и выполните в вашем PostgreSQL клиенте.

### Шаг 3: Запуск проекта

```bash
# Терминал 1: Backend (с Socket.io)
cd backend
npm run dev

# Терминал 2: Frontend
cd frontend
npm run dev
```

Backend запустится на http://localhost:3001  
Frontend запустится на http://localhost:3000

---

## 🧪 Как протестировать новые функции

### Тест 1: "Приступить к работе"

1. **Как заказчик:**
   - Создайте заказ (или войдите в существующий)

2. **Как исполнитель:**
   - Откликнитесь на заказ
   - Подождите, пока заказчик выберет вас

3. **Как заказчик:**
   - Выберите исполнителя из откликов
   - Заказ перейдёт в статус "В работе"

4. **Как исполнитель:**
   - Откройте страницу заказа
   - Увидите кнопку **"Приступить к работе"**
   - Нажмите её
   - ✅ Заказчик получит уведомление
   - ✅ Кнопка изменится на "Заказ выполнен"
   - ✅ Появится чат

### Тест 2: "Отказаться от заказа"

1. **Как исполнитель** (после приступления к работе):
   - Откройте страницу заказа
   - Нажмите **"Отказаться от заказа"**
   - Введите причину отказа (опционально)
   - ✅ Заказ вернётся в статус "Опубликован"
   - ✅ Другие исполнители снова смогут откликнуться
   - ✅ Заказчик получит уведомление с причиной

### Тест 3: Real-time чат

1. **Откройте заказ в статусе "В работе"** (как заказчик или исполнитель)
2. **Проверьте функции чата:**
   - ✅ Отправка сообщений (Enter или кнопка)
   - ✅ Сообщения появляются мгновенно у обоих пользователей
   - ✅ Индикатор "печатает..." (начните печатать)
   - ✅ Статусы доставки (✓ - доставлено, ✓✓ - прочитано)
   - ✅ Автопрокрутка к новым сообщениям
   - ✅ Зелёный индикатор "Онлайн" при подключении
3. **Тест fallback:** Отключите интернет, отправьте сообщение - должно отправиться через REST API при восстановлении соединения

---

## 📊 Новые API Endpoints

### Заказы:

```
POST /api/orders/:id/start-work       # Приступить к работе
POST /api/orders/:id/cancel-work      # Отказаться от заказа
```

### Чат:

```
GET  /api/chat/:orderId/messages           # Получить историю
POST /api/chat/:orderId/messages           # Отправить сообщение
POST /api/chat/:orderId/mark-read          # Отметить прочитанными
GET  /api/chat/:orderId/unread-count       # Счётчик непрочитанных по заказу
GET  /api/chat/unread-count                # Общий счётчик непрочитанных
```

### Socket.io Events:

```
join-order { orderId }         # Присоединиться к комнате заказа
leave-order { orderId }        # Покинуть комнату
send-message { orderId, content, fileUrl }  # Отправить сообщение
typing { orderId }             # Начал печатать
stop-typing { orderId }        # Закончил печатать
mark-read { orderId }          # Отметить прочитанными

# События от сервера:
new-message (message)          # Новое сообщение
user-typing (data)             # Пользователь печатает
user-stop-typing (data)        # Пользователь закончил печатать
messages-read (data)           # Сообщения прочитаны
```

---

## 🗃️ Изменения в базе данных

### Новая таблица:

**messages** - хранит сообщения чата
- `id` - UUID
- `orderId` - связь с заказом
- `senderId` - отправитель
- `content` - текст сообщения
- `fileUrl` - опциональный файл
- `read` - прочитано (boolean)
- `readAt` - время прочтения
- `createdAt`, `updatedAt`

### Обновлённая таблица:

**orders** - добавлено поле:
- `workStartedAt` - временная метка начала работы исполнителем

---

## 🛠️ Возможные проблемы

### Проблема 1: Миграция не применяется

**Ошибка:** `P1000: Authentication failed`

**Решение:**
1. Проверьте DATABASE_URL в `.env`
2. Убедитесь, что PostgreSQL запущена
3. Попробуйте ручную миграцию (см. Вариант B выше)

### Проблема 2: Socket.io не подключается

**Ошибка:** "Disconnected from Socket.io"

**Решение:**
1. Проверьте, что backend запущен
2. Убедитесь, что `NEXT_PUBLIC_API_URL` в `.env.local` frontend указывает на backend
3. Проверьте CORS настройки в `backend/src/config/env.ts`

### Проблема 3: Кнопки не появляются

**Решение:**
1. Очистите кэш браузера
2. Перезапустите frontend (`npm run dev`)
3. Проверьте консоль браузера на ошибки

---

## 📦 Новые зависимости

### Backend:
- `socket.io` - WebSocket сервер для real-time чата
- `@types/socket.io` - TypeScript типы

### Frontend:
- `socket.io-client` - WebSocket клиент
- `date-fns` - форматирование дат (для чата)

---

## ✅ Чеклист готовности

Перед production запуском проверьте:

- [ ] Миграция БД применена успешно
- [ ] Зависимости установлены (backend + frontend)
- [ ] Backend запускается без ошибок
- [ ] Frontend запускается без ошибок
- [ ] Socket.io подключается (зелёный индикатор в чате)
- [ ] Кнопка "Приступить к работе" появляется у исполнителя
- [ ] Кнопка "Отказаться от заказа" появляется у исполнителя
- [ ] Чат работает в real-time
- [ ] Уведомления отправляются корректно

---

## 📚 Дополнительная документация

- **Общий обзор:** `README.md`
- **Быстрый старт:** `QUICKSTART.md`
- **Production деплой:** `DEPLOYMENT.md`
- **Безопасность:** `SECURITY.md`
- **Детали Этапа 8:** `STAGE8_COMPLETE.md`

---

## 🎉 Платформа готова на 100%!

Все функции из технического задания реализованы! 🚀

**Следующий шаг:** Production деплой на VPS (см. `DEPLOYMENT.md`)

**Вопросы?** Проверьте документацию или откройте issue в репозитории.

---

**УСПЕХОВ С ЗАПУСКОМ! 💪**

