# 🔐 Политика безопасности Montaj Platform

## Поддерживаемые версии

| Версия | Поддержка         |
| ------ | ----------------- |
| 1.0.x  | :white_check_mark:|

## Сообщение об уязвимости

Если вы обнаружили уязвимость в системе безопасности:

1. **НЕ создавайте публичный issue**
2. Отправьте email на: security@your-domain.com
3. Включите:
   - Описание уязвимости
   - Шаги для воспроизведения
   - Потенциальное влияние
   - Предложения по исправлению (опционально)

### Что ожидать

- **Подтверждение:** В течение 48 часов
- **Первый ответ:** В течение 5 рабочих дней
- **Исправление:** В зависимости от серьёзности (1-30 дней)

## Реализованные меры безопасности

### 🔒 Аутентификация и авторизация

- ✅ JWT токены с истечением срока
- ✅ Bcrypt хеширование паролей (10 rounds)
- ✅ SMS верификация телефонов
- ✅ Role-based access control (RBAC)
- ✅ Защита от bruteforce (rate limiting)

### 🛡️ Защита данных

- ✅ HTTPS (TLS 1.2+)
- ✅ Encrypted cookies
- ✅ Secure headers (Helmet.js)
- ✅ CORS настройка
- ✅ SQL injection защита (Prisma ORM)
- ✅ XSS защита (sanitize-html)

### 🚦 Rate Limiting

- API: 100 запросов / 15 минут
- Login: 5 попыток / минута
- SMS: 3 попытки / час

### 🔐 Secrets Management

- Все secrets в environment variables
- Никаких хардкоженных ключей
- .env файлы в .gitignore

### 📝 Логирование

- Логирование всех действий администраторов
- Логирование неудачных попыток входа
- Ротация логов (10MB, 3 файла)

### 🔄 Регулярные обновления

- Автоматические обновления зависимостей (Dependabot)
- Мониторинг CVE через npm audit
- Регулярные проверки безопасности

## Рекомендации для продакшена

### Обязательные

1. **Сгенерируйте сильные пароли:**
```bash
openssl rand -base64 32
```

2. **Настройте firewall:**
```bash
sudo ufw allow 22/tcp    # SSH
sudo ufw allow 80/tcp    # HTTP
sudo ufw allow 443/tcp   # HTTPS
sudo ufw enable
```

3. **Настройте автоматические бэкапы:**
```bash
# Добавить в crontab
0 3 * * * /var/www/montaj/scripts/backup.sh
```

4. **Включите SSL:**
```bash
./scripts/ssl-setup.sh your-domain.com admin@your-domain.com
```

### Рекомендуемые

1. **Fail2ban для SSH:**
```bash
sudo apt install fail2ban
sudo systemctl enable fail2ban
```

2. **Мониторинг:**
- Sentry для отслеживания ошибок
- Prometheus + Grafana для метрик

3. **WAF (Web Application Firewall):**
- Cloudflare
- AWS WAF
- ModSecurity

4. **2FA для админов:**
- Google Authenticator
- SMS верификация

5. **Regular security audits:**
```bash
npm audit
docker scan
```

## Контрольный список перед продакшеном

- [ ] Все .env файлы настроены
- [ ] JWT_SECRET изменён
- [ ] Пароли БД изменены
- [ ] SSL сертификаты установлены
- [ ] CORS настроен правильно
- [ ] Rate limiting включен
- [ ] Логирование настроено
- [ ] Бэкапы настроены
- [ ] Мониторинг настроен
- [ ] Firewall настроен
- [ ] npm audit без критических уязвимостей
- [ ] Тесты проходят
- [ ] Health checks работают

## Известные ограничения

1. **SMS верификация:** Требует настройки SMSC.ru
2. **Email:** Требует SMTP сервера
3. **Payments:** Требует YooKassa аккаунта

## Контакты

- Email: security@your-domain.com
- Responsible disclosure welcome
- Bug bounty program: coming soon

## История изменений

### 2026-02-09
- Первый релиз
- Базовая система безопасности
- Rate limiting
- JWT authentication

